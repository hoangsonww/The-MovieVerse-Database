input {
  beats {
    port => 5044
  }
}

filter {
  json {
    source => "message"
    target => "json"
    skip_on_invalid_json => true
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "movieverse-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
